# Sample local secrets store for `${local:path/to/secret}` lookups.
#
# Do not commit real secrets. Use placeholder/demo values only.
api:
  token: demo-api-token

database:
  host: localhost
  port: 5432
  credentials:
    username: demo_user
    password: demo_password

services:
  - name: billing
    apiKey: billing-key-1
  - name: analytics
    apiKey: analytics-key-1

